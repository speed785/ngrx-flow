import { DebugElement } from '@angular/core';
import { QueryOptions, QueryType } from '../types';
export declare function getChildren<R>(debugElementRoot: DebugElement): (directiveOrSelector: QueryType, options?: QueryOptions<R>) => R[];
export declare function setProps<T, K extends string | number | symbol, V>(instance: T, key: K, value: V, firstChange?: boolean): T & {
    [KEY in K]: V;
};
export declare function setProps<T, KV>(instance: T, keyValues?: KV): T & KV;
