import { DebugElement } from '@angular/core';
import { ComponentFixture } from '@angular/core/testing';
import { BaseSpectator } from '../base/base-spectator';
import { HostComponent } from '../spectator-host/host-component';
/**
 * @publicApi
 */
export declare class SpectatorPipe<P, H = HostComponent> extends BaseSpectator {
    hostComponent: H;
    fixture: ComponentFixture<any>;
    debugElement: DebugElement;
    element: Element;
    constructor(hostComponent: H, fixture: ComponentFixture<any>, debugElement: DebugElement, element: Element);
    detectChanges(): void;
    setHostInput<K extends keyof H>(input: Partial<H>): void;
    setHostInput<K extends keyof H>(input: K, inputValue: H[K]): void;
}
