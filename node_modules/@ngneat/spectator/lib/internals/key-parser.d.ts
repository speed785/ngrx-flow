import { KeyboardEventOptions } from '../types';
export interface ModifierKeys {
    alt?: boolean;
    control?: boolean;
    shift?: boolean;
    meta?: boolean;
}
export interface KeyOptions {
    key: string | false;
    keyCode: number | false;
    modifiers: ModifierKeys;
}
export declare const parseKeyOptions: (keyOrKeyCode: string | number | KeyboardEventOptions) => KeyOptions;
