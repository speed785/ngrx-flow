import { BuilderContext, BuilderOutput } from '@angular-devkit/architect';
import { JsonObject } from '@angular-devkit/core';
import { Observable } from 'rxjs';
export interface StorybookConfig extends JsonObject {
    configFolder?: string;
    configPath?: string;
    pluginPath?: string;
    srcRoot?: string;
}
export interface StorybookBuilderOptions extends JsonObject {
    uiFramework: string;
    config: StorybookConfig;
    quiet?: boolean;
    outputPath?: string;
    docsMode?: boolean;
}
declare const _default: import("@angular-devkit/architect/src/internal").Builder<StorybookBuilderOptions>;
export default _default;
/**
 * @whatItDoes This is the starting point of the builder.
 * @param builderConfig
 */
export declare function run(options: StorybookBuilderOptions, context: BuilderContext): Observable<BuilderOutput>;
